<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobidev.mpesa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MpesaActivity" />
        <activity
            android:name=".PhoneNoActivity"
            android:label="@string/title_activity_phone_no"></activity>
        <activity android:name=".SendMoneyActivity"></activity>
        <activity android:name=".AmountActivity"></activity>
        <activity android:name=".PinActivity"></activity>
    </application>

</manifest>
